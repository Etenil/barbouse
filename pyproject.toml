[tool.pyflow]
name = ""
py_version = "3.7"
version = "0.1.0"

[tool.pyflow.scripts]
barbouse = "barbouse.barbouse:main"

[tool.pyflow.dependencies]
requests = "^2.25.1"
jq = "^1.1.3"
pygments = "^2.9.0"

[tool.pyflow.dev-dependencies]
